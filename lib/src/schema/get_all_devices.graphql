query GetAllDevices {
	deviceInfo {
    deviceName
    settable
    units
    history {
      totalPoints
      firstPoint {
        stamp
        boolValue
        intValue
        floatValue
        stringValue
        colorValue
      }
      lastPoint {
        stamp
        boolValue
        intValue
        floatValue
        stringValue
        colorValue
      }
    }
    driver {
      name
    }
  }
}